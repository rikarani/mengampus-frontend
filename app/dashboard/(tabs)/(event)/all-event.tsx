import { StyledSafeAreaView } from "@/components/styled/StyledSafeAreaView";
import { Text } from "react-native";

export default function AllEventPage() {
  return (
    <StyledSafeAreaView>
      <Text>All Event Page</Text>
    </StyledSafeAreaView>
  );
}
